This folder contains raw TED contract award source files for 2018–2023.

The pipeline expects annual files in `data/raw/tedcan/`.
These raw files are not tracked in Git because they are large and can be downloaded by running `R/01_download.R`.