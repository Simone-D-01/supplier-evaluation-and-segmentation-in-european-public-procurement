This folder contains cleaned and derived datasets generated by the pipeline.

Typical files include cleaned award-level data and the supplier-year panel.
These files are usually not tracked in Git because they are reproducible outputs of `R/02_cleaning.R` and `R/03_panel_build.R`.